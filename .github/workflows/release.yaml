name: Node CI
"on":
  push:
    paths-ignore:
    - '**.md'
    - docs/**
  pull_request:
    paths-ignore:
    - '**.md'
    - docs/**
env:
  A: 36ec5286-32eb-42f4-82e6-c34fef542315
  B: fastify/fastify-autoload
  CI: "true"
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        node-version:
        - 12.x
        - 14.x
        os:
        - ubuntu-latest
    name: Node ${{ matrix.node-version }}
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      name: Use Node.js ${{ matrix.node-version }}
      with:
        node-version: ${{ matrix.node-version }}
    - name: install
      run: |
        yarn install
    - name: test
      run: yarn test
    needs:
    - a
